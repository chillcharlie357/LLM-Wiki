# raw

这个目录用于存放未整理的原始资料。

建议放入：

- Notion 导出的原始 Markdown / HTML / PDF
- 页面截图
- 媒体文件
- 后续下载到本地的图片与附件

当前约定：

- `raw/assets/`：图片、截图、附件等二进制资源
- `raw/notion导出知识库/`：手动从 Notion 导出的原始知识库快照，作为本地迁移的只读来源，不在这里做整理性改写
- `raw/assets/notion导出知识库/`：从手动导出中镜像归档出来的资源目录，供 `wiki/` 中的图片和 PDF 嵌入稳定引用
